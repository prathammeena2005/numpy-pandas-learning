import numpy as np
 
arr = np.array([1, 4, 9, 16, 25])# np.sqrt() is used to computes the square rootnof each element in the array.
print(np.sqrt(arr))      

# np.exp() is used to computes the exponential of each element in the array.
print(np.exp(arr))       